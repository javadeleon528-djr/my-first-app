# 第一版学习指南

这一版先验证桌面端完整交互：导入图片、拖动框选、选择模型、执行、显示结果、保存结果。模型尚未部署，所以当前输出是**模拟分割结果**，仅用于验证软件流程，不能用于医学诊断。

## 先认识文件夹

```text
desktop_medsam/
├─ app.py             # 主程序：界面、鼠标框选、状态、模拟推理、保存结果
├─ config.json        # 将来填写网络 API 地址、模型路径、结果目录
├─ requirements.txt   # 本项目需要的 Python 第三方库
├─ README.md          # 如何启动及后续接入模型的说明
├─ 学习指南.md        # 本文件
└─ build_exe.bat      # 最后需要打包时才使用；现在不用运行
```

`vendor/`、`.venv/`、`build/`、`dist/` 是开发或打包过程中产生的辅助内容，学习第一版时可以先忽略。

## 推荐学习顺序

1. 打开 `app.py`，先看 `MedSAMApp._build_ui()`：它对应界面的左侧工作台、中间待分割图、右侧结果图和底部状态栏。
2. 看 `choose_file()`、`drop_file()` 与 `load_file()`：它们完成点击选文件和拖入文件。
3. 看 `box_press()`、`box_drag()`、`box_release()`：它们把鼠标在画布上的框转换成原始影像坐标。
4. 看 `run_segmentation()`：点击“开始执行”后启动后台任务，避免界面卡住。
5. 看 `ModelGateway`：这是唯一要替换为真实 MedSAM2 或网络 API 的位置。
6. 看 `save_result()`：它把右侧结果保存到 `D:\Download`。

## 模型部署后只改哪里

| 目标 | 修改位置 | 要做的事 |
|---|---|---|
| 使用本地 MedSAM2 | `ModelGateway._local_medsam2()` | 读取模型权重，输入原图与框坐标，返回分割掩膜/叠加图 |
| 调用网络模型 | `ModelGateway._remote_api()` | 向 `config.json` 中的接口上传原图与框坐标，读取返回掩膜 |
| 更改服务器地址 | `config.json` | 填写真实 `remote_api_url`，不要把密钥写进程序源码 |

## 当前运行方式

在此文件夹中运行 `python app.py`。如果尚未安装拖放组件，点击“导入文件”仍可使用；完整拖放需要安装 `requirements.txt` 内的依赖。

等你确认交互与真实模型接口都符合预期后，再运行 `build_exe.bat` 打包。
